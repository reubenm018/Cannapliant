// REPLACE THIS
// Paste the contents of your compliance-checker component here.
// After pasting, all fetch calls to https://api.anthropic.com/v1/messages
// will be updated to use the proxied API_URL below, and the
// anthropic-beta header will be removed.
//
// const API_URL = import.meta.env.VITE_API_URL
//   ? `${import.meta.env.VITE_API_URL}/api/messages`
//   : '/api/messages';

export default function App() {
  return <div>Paste compliance-checker component here.</div>;
}
